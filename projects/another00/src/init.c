#include "ush.h"

int init(void) {
    // if (signal(SIGINT, SIG_IGN))
    //     printf("\ncan't catch SIGINT\n");
    // if (signal(SIGTSTP, SIG_IGN))
    //     printf("\ncan't catch SIGINT\n");
    return 0;
}
